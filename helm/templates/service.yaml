apiVersion: v1
kind: Service
metadata:
  name: ms-communication
  labels:
    app: ms-communication
spec:
  selector:
    app: ms-communication
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
